#!/bin/sh
# This script is meant to shorten the syntax for calling eQual controllers through CLI
if php -v > /dev/null 2>&1; then
    php run.php "$@"
else
    echo "PHP cannot be found. Check your installation."
fi