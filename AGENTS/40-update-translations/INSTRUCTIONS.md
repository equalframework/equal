1. Identify all supported languages in `i18n/{lang}/`.
2. Locate the translation file matching the entity namespace.
3. Add or update root keys: `name`, `plural`, and `description`.
4. Add or update `model` entries for every impacted field.
5. Ensure each impacted field has `label`, `description`, and `help`.
6. Add `selection` labels when relevant.
7. Add or update `view` entries.
8. Add layout section labels.
9. Add action labels and descriptions.
10. Add route labels.
11. Add export labels.
12. Add business error messages.
13. Preserve exact technical keys from classes and views.
14. Use clear, concise, professional language.
15. Do not omit intentionally empty keys; use `""` when needed.
16. Refer to `AGENTS/AGENTS_REFERENCE.md` for the complete i18n structure.
