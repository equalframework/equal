Options -Indexes
DirectoryIndex index.php
CGIPassAuth on 

<IfModule mod_rewrite.c>

    RewriteEngine On
    RewriteBase /
    RewriteRule ^(index|console)\.php(\??.*)$ - [L]

    RewriteCond %{REQUEST_FILENAME} ^(.*)\.php$ [OR]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.php [L]

</IfModule>